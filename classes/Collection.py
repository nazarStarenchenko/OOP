from abc import ABC , abstractmethod

class Collection(ABC):
    @abstractmethod
    def getAllFromDataBase(self):
        ItemCollection()
        UsersCollection()
        eval(input(r"Lists:Collection.__allUsers;Collection.__allItems;"))

